#include "main.h"


/**
 * *_abs - finds absolute value of an integer
 * @n: argument
 * Description: Finds absolute value of an integer
 * Return: Always 0
 */

int _abs(int n)
{
	if (n < 0)
	{
		abs(n);
		_putchar(n);
	}
	else if (n > 0)
	{
		abs(n);
		_putchar(n);
	}
	else
	{
		abs(n);
		_putchar(n);
	}
	return (0);
}
		
