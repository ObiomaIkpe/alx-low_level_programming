#include "main.h"

/**
 * mul - function to multiply two integers
 * @a: first argument
 * @b: secodn argument
 * Return: product
 */
int mul(int a, int b)
{
	int product;

	product = a * b;

	return (product);
}
